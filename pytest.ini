[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --color=yes
markers =
    integration: marks tests as integration tests (may download actual packages)
    slow: marks tests as slow running
