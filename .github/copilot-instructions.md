---
applyTo: "**"
---
When running unit tests, call `spack unit-test --extension=helpers`, after which regular PyTest arguments may be added. Assume that Spack is already present and active in the shell environment and that this extension is accessible to it.
